---
interface Props {
  animationClass?: string
}

const { animationClass } = Astro.props
---

<p
  class:list={[
    'text-gray-300 text-lg text-center my-4 mx-auto',
    animationClass ? animationClass : null,
  ]}
>
  <slot />
</p>
