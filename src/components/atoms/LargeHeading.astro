---
interface Props {
  animationClass?: string
}

const { animationClass } = Astro.props
---

<h1
  class:list={[
    'text-[4rem] md:text-[5rem] font-bold leading-none tracking-tighter',
    `${animationClass ? animationClass : null}`,
  ]}
>
  <slot />
</h1>
